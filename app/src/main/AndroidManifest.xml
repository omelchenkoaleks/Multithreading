<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.omelchenkoaleks.multithreading">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="._006_handler_test_methods.TestMethodsActivity"></activity>
        <activity android:name="._005_handler_runnable.HandlerRunnableActivity" />
        <activity android:name="._004_handler_delayd_remove_callback.DelayedActivity" />
        <activity android:name="._003_handler_arg1_arg2_obj.HandlerAttributeActivity" />
        <activity android:name="._002_handler_want.WantActivity" />
        <activity android:name="._001_handler.HandlerActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>